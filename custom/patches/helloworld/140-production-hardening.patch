diff --git a/luci-app-ssr-plus/luasrc/controller/shadowsocksr.lua b/luci-app-ssr-plus/luasrc/controller/shadowsocksr.lua
index 6a5a01b..cbb8cd0 100644
--- a/luci-app-ssr-plus/luasrc/controller/shadowsocksr.lua
+++ b/luci-app-ssr-plus/luasrc/controller/shadowsocksr.lua
@@ -328,6 +328,19 @@ local function clash_process_running()
 	return luci.sys.call("(busybox ps -w 2>/dev/null || busybox ps) | grep ssr-retcp | grep -v grep >/dev/null") == 0
 end
 
+local function default_tcp_listener_running()
+	local port = tonumber(uci:get_first("shadowsocksr", "global", "default_node_local_port", "1234"))
+	if not port then
+		return false
+	end
+	local command = string.format(
+		"busybox netstat -lnt 2>/dev/null | awk -v port=%s " ..
+		"'$4 ~ (\":\" port \"$\") && $6 == \"LISTEN\" { found=1 } END { exit !found }'",
+		luci.util.shellquote(tostring(port))
+	)
+	return luci.sys.call(command) == 0
+end
+
 local function global_client_running()
 	local process_list = luci.sys.exec("busybox ps -w 2>/dev/null || busybox ps")
 	local global_server = uci:get_first("shadowsocksr", "global", "global_server", "nil")
@@ -340,7 +353,11 @@ local function global_client_running()
 		return true
 	end
 
-	if (global_type == "clash" or global_type == "v2ray" or global_type == "tuic" or global_type == "ss" or global_type == "ss-rust")
+	if global_type == "v2ray" and process_list:find("ssr%-retcp") then
+		return default_tcp_listener_running()
+	end
+
+	if (global_type == "clash" or global_type == "tuic" or global_type == "ss" or global_type == "ss-rust")
 		and process_list:find("ssr%-retcp") then
 		return true
 	end
diff --git a/luci-app-ssr-plus/luasrc/model/cbi/shadowsocksr/status.lua b/luci-app-ssr-plus/luasrc/model/cbi/shadowsocksr/status.lua
index b037e20..51ca86e 100644
--- a/luci-app-ssr-plus/luasrc/model/cbi/shadowsocksr/status.lua
+++ b/luci-app-ssr-plus/luasrc/model/cbi/shadowsocksr/status.lua
@@ -18,6 +18,17 @@ local Process_list = luci.sys.exec("busybox ps -w 2>/dev/null || busybox ps")
 local uci = require "luci.model.uci".cursor()
 local global_server = uci:get_first("shadowsocksr", "global", "global_server", "nil")
 local global_type = global_server ~= "nil" and (uci:get("shadowsocksr", global_server, "type") or "") or ""
+local global_tcp_port = tonumber(uci:get_first("shadowsocksr", "global", "default_node_local_port", "1234"))
+local function global_tcp_listener_running()
+	if not global_tcp_port then
+		return false
+	end
+	return luci.sys.call(string.format(
+		"busybox netstat -lnt 2>/dev/null | awk -v port=%d " ..
+		"'$4 ~ (\":\" port \"$\") && $6 == \"LISTEN\" { found=1 } END { exit !found }'",
+		global_tcp_port
+	)) == 0
+end
 local global_socks_enabled = uci:get_first("shadowsocksr", "socks5_proxy", "enabled", "0") == "1"
 local global_socks_server = uci:get_first("shadowsocksr", "socks5_proxy", "server", "nil")
 local global_http_enabled = uci:get_first("shadowsocksr", "http_proxy", "enabled", "0") == "1"
@@ -122,7 +133,13 @@ if global_type == "socks5" and Process_list:find("ipt2socks") then
 	end
 end
 
-if (global_type == "clash" or global_type == "v2ray" or global_type == "tuic" or global_type == "ss" or global_type == "ss-rust")
+if global_type == "v2ray" and Process_list:find("ssr%-retcp") and global_tcp_listener_running() then
+	-- Xray uses separate TCP REDIRECT and UDP TPROXY processes. The UDP
+	-- status is determined by udp-only-ssr-reudp above, not by the TCP process.
+	redir_run = 1
+end
+
+if (global_type == "clash" or global_type == "tuic" or global_type == "ss" or global_type == "ss-rust")
 	and Process_list:find("ssr%-retcp") then
 	redir_run = 1
 	reudp_run = 1
diff --git a/luci-app-ssr-plus/root/etc/init.d/shadowsocksr b/luci-app-ssr-plus/root/etc/init.d/shadowsocksr
index 4440289..9881f33 100755
--- a/luci-app-ssr-plus/root/etc/init.d/shadowsocksr
+++ b/luci-app-ssr-plus/root/etc/init.d/shadowsocksr
@@ -1530,14 +1530,14 @@ get_name() {
 gen_config_file() { #server1 type2 code3 local_port4 socks_port5 chain6 threads5
 	local server="$1"
 	local type="$2"
-	local mode="$3"
+	local code="$3"
 	local local_port="$4"
 	local socks_port="$5"
 	local chain="$6"
 	local tmp_port
 	local ss_protocol
 
-	case "$mode" in
+	case "$code" in
 	1)
 		config_file=$tcp_config_file
 		chain_config_file=$(echo "${config_file}" | sed 's/ssrplus\//ssrplus\/chain-/')
@@ -1564,7 +1564,7 @@ gen_config_file() { #server1 type2 code3 local_port4 socks_port5 chain6 threads5
 	case "$type" in
 	ss | ssr)
 		lua /usr/share/shadowsocksr/gen_config.lua "$server" "$mode" "$local_port" "${ss_protocol:-redir}" >"$config_file"
-		if [ "$mode" = "3" ]; then
+		if [ "$code" = "3" ]; then
 			lua /usr/share/shadowsocksr/gen_config.lua "$server" "$mode" "$tmp_port" socks >"$shunt_dns_config_file"
 		fi
 		;;
@@ -1572,7 +1572,7 @@ gen_config_file() { #server1 type2 code3 local_port4 socks_port5 chain6 threads5
 		lua /usr/share/shadowsocksr/gen_config.lua "$server" "$mode" "$local_port" "$socks_port" >"$config_file"
 		;;
 	trojan)
-		case "$mode" in
+		case "$code" in
 		1)
 			lua /usr/share/shadowsocksr/gen_config.lua "$server" nat "$local_port" >"$config_file"
 			;;
@@ -1589,7 +1589,7 @@ gen_config_file() { #server1 type2 code3 local_port4 socks_port5 chain6 threads5
 		esac
 		;;
 	naiveproxy)
-		case "$mode" in
+		case "$code" in
 		1)
 			lua /usr/share/shadowsocksr/gen_config.lua "$server" redir "$local_port" >"$config_file"
 			;;
@@ -1606,7 +1606,7 @@ gen_config_file() { #server1 type2 code3 local_port4 socks_port5 chain6 threads5
 		lua /usr/share/shadowsocksr/gen_config.lua "$server" "$mode" "$local_port" "$socks_port" >"$config_file"
 		;;
 	tuic)
-		case "$mode" in
+		case "$code" in
 		1|2|4)
 			lua /usr/share/shadowsocksr/gen_config.lua "$server" "$mode" "$local_port" >"$config_file"
 			;;
@@ -1620,7 +1620,7 @@ gen_config_file() { #server1 type2 code3 local_port4 socks_port5 chain6 threads5
 		esac
 		;;
 	shadowtls)
-		case "$mode" in
+		case "$code" in
 		1|2|4)
 			if [ -z "$chain" ]; then
 				lua /usr/share/shadowsocksr/gen_config.lua "$server" "$type" "$local_port" >"$chain_config_file"
@@ -1696,6 +1696,10 @@ start_udp() {
 			xray_bin="$(first_type xray)"
 			if [ -n "$xray_bin" ]; then
 				gen_config_file "$UDP_RELAY_SERVER" "$type" 2 "$tmp_udp_port"
+				if ! "$xray_bin" run -test -c "$udp_config_file" >/dev/null 2>&1; then
+					echolog "UDP TPROXY Relay:Xray configuration validation failed."
+					return 1
+				fi
 				ln_start_bin "$(first_type xray)" v2ray run -c "$udp_config_file"
 				echolog "UDP TPROXY Relay:$($(first_type xray) version | head -1) Started!"
 			else
@@ -2145,10 +2149,12 @@ get_udp_relay_mode() {
 			;;
 		socks)
 			socks_ver=$(uci_get_by_name "$node" socks_ver 5)
-			[ "$socks_ver" = "5" ] && echo "native" || echo "disabled"
+			[ "$socks_ver" = "5" ] && echo "split" || echo "disabled"
 			;;
 		*)
-			echo "native"
+			# Xray TCP uses REDIRECT while UDP uses TPROXY. Keep separate
+			# inbounds so each socket receives the correct transparent mode.
+			echo "split"
 			;;
 		esac
 		;;
@@ -2463,7 +2469,7 @@ load_config() {
 		udp_config_file="$TMP_PATH/udp-only-ssr-reudp.json"
 		ARG_UDP="-U"
 		ARG_UDP_RULES=""
-		start_udp
+		start_udp || return 1
 		mode="tcp"
 		;;
 	esac
@@ -2958,11 +2964,16 @@ enter_fail_open() {
 
 main_proxy_runtime_healthy() {
 	local attempt=0
+	local tcp_port
+
+	tcp_port="$(get_default_node_local_port)"
 
 	[ "$redir_tcp" -gt 0 ] || return 0
 	[ -n "$tcp_config_file" ] || return 1
 	while [ "$attempt" -lt 5 ]; do
-		if ps_list | grep -F "$tcp_config_file" | grep -v grep >/dev/null 2>&1; then
+		if ps_list | grep -F "$tcp_config_file" | grep -v grep >/dev/null 2>&1 && \
+			busybox netstat -lnt 2>/dev/null | awk -v port="$tcp_port" \
+				'$4 ~ (":" port "$") && $6 == "LISTEN" { found=1 } END { exit !found }'; then
 			return 0
 		fi
 		attempt=$((attempt + 1))
diff --git a/luci-app-ssr-plus/root/usr/bin/ssr-monitor b/luci-app-ssr-plus/root/usr/bin/ssr-monitor
index 77fb77e..d282705 100755
--- a/luci-app-ssr-plus/root/usr/bin/ssr-monitor
+++ b/luci-app-ssr-plus/root/usr/bin/ssr-monitor
@@ -62,8 +62,28 @@ get_expected_threads() {
 	echo "$threads"
 }
 
+tcp_listener_running() {
+	local port
+	port="$(get_default_node_local_port)"
+	busybox netstat -lnt 2>/dev/null | awk -v port="$port" \
+		'$4 ~ (":" port "$") && $6 == "LISTEN" { found=1 } END { exit !found }'
+}
+
+proxy_path_health_required() {
+	[ -n "$GLOBAL_SERVER" ] && [ "$GLOBAL_SERVER" != "nil" ] && \
+		[ "$(uci_get_by_type access_control router_proxy 1)" = "1" ]
+}
+
+proxy_path_healthy() {
+	curl -4 -kfsS --connect-timeout 4 --max-time 8 -o /dev/null \
+		"https://www.gstatic.com/generate_204" >/dev/null 2>&1 || \
+	curl -4 -kfsS --connect-timeout 4 --max-time 8 -o /dev/null \
+		"https://cp.cloudflare.com/" >/dev/null 2>&1
+}
+
 FAIL_STATE_FILE="/tmp/ssrplus-monitor.failures"
 MAX_CONSECUTIVE_FAILURES=3
+PROXY_HEALTH_CHECK_EVERY=4
 
 recover_or_fail_open() {
 	local reason="$1"
@@ -108,6 +128,7 @@ server_port=$(uci_get_by_name $GLOBAL_SERVER server_port)
 password=$(uci_get_by_name $GLOBAL_SERVER kcp_password)
 kcp_param=$(uci_get_by_name $GLOBAL_SERVER kcp_param)
 [ "$password" != "" ] && password="--key "${password}
+proxy_health_tick=0
 
 while [ "1" == "1" ]; do #死循环
 	sleep 000030s
@@ -126,6 +147,9 @@ while [ "1" == "1" ]; do #死循环
 			echolog "ssrplus redir tcp error.restart!"
 			recover_or_fail_open "main TCP proxy process is missing"
 		fi
+		if ! tcp_listener_running; then
+			recover_or_fail_open "main TCP transparent proxy listener is unavailable"
+		fi
 	fi
 	#redir udp
 	if [ "$redir_udp_process" -gt 0 ]; then
@@ -214,5 +238,16 @@ while [ "1" == "1" ]; do #死循环
 			fi
 		fi
 	fi
-	rm -f "$FAIL_STATE_FILE"
+	proxy_health_tick=$((proxy_health_tick + 1))
+	if proxy_path_health_required; then
+		if [ "$proxy_health_tick" -ge "$PROXY_HEALTH_CHECK_EVERY" ]; then
+			proxy_health_tick=0
+			if ! proxy_path_healthy; then
+				recover_or_fail_open "transparent proxy data path is unavailable"
+			fi
+			rm -f "$FAIL_STATE_FILE"
+		fi
+	else
+		rm -f "$FAIL_STATE_FILE"
+	fi
 done
diff --git a/luci-app-ssr-plus/root/usr/share/shadowsocksr/gen_config.lua b/luci-app-ssr-plus/root/usr/share/shadowsocksr/gen_config.lua
index 5e85894..93765d7 100755
--- a/luci-app-ssr-plus/root/usr/share/shadowsocksr/gen_config.lua
+++ b/luci-app-ssr-plus/root/usr/share/shadowsocksr/gen_config.lua
@@ -415,6 +415,11 @@ if local_port ~= "0" then
 			port = tonumber(local_port),
 			protocol = "dokodemo-door",
 			settings = {network = proto, followRedirect = true},
+			streamSettings = {
+				sockopt = {
+					tproxy = (proto == "udp") and "tproxy" or "redirect"
+				}
+			},
 			sniffing = {
 				enabled = true,
 				destOverride = {"http", "tls", "quic"},
@@ -1135,7 +1140,9 @@ local chain_vmess = {
 				followRedirect = true
 			},
 			streamSettings = {
-				sockopt = {tproxy = "redirect"}
+				sockopt = {
+					tproxy = (proto == "udp") and "tproxy" or "redirect"
+				}
 			},
 			sniffing = {
 				enable = true,
diff --git a/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua b/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua
index 4f4822d..4997a82 100755
--- a/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua
+++ b/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua
@@ -2029,6 +2029,7 @@ local function read_old_groupHash(url)
 	if old_groupHash_cache[url] then
 		return old_groupHash_cache[url]
 	end
+	local wanted_url_hash = md5(url)
 
 	local files = nixio.fs.dir("/tmp/") or {}
     for file in files do
@@ -2038,8 +2039,14 @@ local function read_old_groupHash(url)
 				local content = trim(nixio.fs.readfile(full_path) or "")
 				if content ~= "" then
 					local ok, data = pcall(jsonParse, content)
-					if ok and data and data.url and data.url == url then
-						return data.hash, data.url
+					if ok and data then
+						local cached_url_hash = data.url_hash
+						if not cached_url_hash and data.url then
+							cached_url_hash = md5(data.url)
+						end
+						if cached_url_hash == wanted_url_hash then
+							return data.hash, wanted_url_hash
+						end
 					end
 				end
 			end
@@ -2050,14 +2057,16 @@ end
 
 -- 将订阅分组最新内容的 MD5 值保存到对应的临时文件中，以便下次更新时进行对比。
 -- 同时保存 groupHash 和 url，用于检测 groupHash 的值变化
-local function write_new_md5(groupHash, md5, url)
+local function write_new_md5(groupHash, content_md5, url)
 	local path = get_md5_path(groupHash)
 	local data = {
-		md5 = md5,
+		md5 = content_md5,
 		hash = groupHash,
-		url = url
+		url_hash = md5(url)
 	}
 	nixio.fs.writefile(path, jsonStringify(data))
+	-- Nixio parses a numeric mode as decimal octal digits (for example 600).
+	nixio.fs.chmod(path, 600)
 end
 
 -- curl
@@ -2598,11 +2607,11 @@ local execute = function()
 		
 		log("raw 长度: "..#raw)
 		local old_md5 = read_old_md5(groupHash)
+		local subscription_label = "subscription[" .. tostring(groupHash):sub(1, 12) .. "]"
 
-		log("处理订阅: " .. url)
+		log("处理订阅: " .. subscription_label)
 		if should_use_mihomo_mode() and converted then
-			log("使用订阅转换模板: " .. template_url)
-			log("转换服务: " .. convert_address)
+			log(subscription_label .. ": 使用订阅转换")
 		end
 		if should_use_mihomo_mode() and yaml_removed_count and yaml_removed_count > 0 then
 			log("Clash/Mihomo YAML 已按订阅过滤关键词移除节点数量: " .. tostring(yaml_removed_count))
@@ -2616,10 +2625,10 @@ local execute = function()
 		local missing_saved_nodes = old_md5 and new_md5 == old_md5 and not group_has_saved_nodes(groupHash)
 		
 		if #raw == 0 then
-			log(url .. ': 获取内容为空')
+			log(subscription_label .. ': 获取内容为空')
 			loadOldNodes(groupHash)
 		elseif old_md5 and new_md5 == old_md5 and not backend_refresh and not mihomo_subscribe_refresh and not missing_saved_nodes then
-			log("订阅未变化, 跳过无需更新的订阅: " .. url)
+			log("订阅未变化, 跳过无需更新的订阅: " .. subscription_label)
 			-- 防止 diff 阶段误删未更新订阅节点
 			loadOldNodes(groupHash)
 			--ucic:foreach(name, uciType, function(s)
@@ -2630,13 +2639,13 @@ local execute = function()
 			--end)
 		else
 			if backend_refresh and old_md5 and new_md5 == old_md5 then
-				log("检测到 SS 后端偏好变化，强制重建订阅节点: " .. url)
+				log("检测到 SS 后端偏好变化，强制重建订阅节点: " .. subscription_label)
 			end
 			if mihomo_subscribe_refresh and old_md5 and new_md5 == old_md5 then
-				log("检测到可迁移订阅节点，强制重建为 Mihomo 总节点: " .. url)
+				log("检测到可迁移订阅节点，强制重建为 Mihomo 总节点: " .. subscription_label)
 			end
 			if missing_saved_nodes then
-				log("订阅内容未变化但本地节点不存在，强制重建订阅节点: " .. url)
+				log("订阅内容未变化但本地节点不存在，强制重建订阅节点: " .. subscription_label)
 			end
 			updated = true
 
@@ -2669,7 +2678,7 @@ local execute = function()
 					cache[groupHash][result.hashkey] = result
 					log('成功导入 Clash 总节点: ' .. result.alias)
 				else
-					log('丢弃无效 Clash 总节点: ' .. url)
+					log('丢弃无效 Clash 总节点: ' .. subscription_label)
 				end
 			-- SSD 似乎是这种格式 ssd:// 开头的
 			elseif raw:find('ssd://') then
@@ -2983,6 +2992,14 @@ if subscribe_items and #subscribe_items > 0 then
 		log("订阅模式: 传统节点模式")
 	end
 	
+	-- Keep the pre-update UCI file and service state so any update error can be
+	-- rolled back without taking a working proxy offline.
+	local config_path = "/etc/config/" .. name
+	local config_snapshot = nixio.fs.readfile(config_path)
+	local service_was_running = luci.sys.call(
+		"/etc/init.d/" .. name .. " running > /dev/null 2>&1"
+	) == 0
+
 	local direct_bypass = create_direct_subscribe_bypass()
 	if direct_bypass then
 		direct_bypass:apply()
@@ -2991,13 +3008,21 @@ if subscribe_items and #subscribe_items > 0 then
 		log(e)
 		log(debug.traceback())
 		log('发生错误, 正在尝试恢复服务状态')
-		local firstServer = ucic:get_first(name, uciType)
-		if firstServer then
-			luci.sys.call("/etc/init.d/" .. name .. " restart > /dev/null 2>&1 &") -- 不加&的话日志会出现的更早
-			log('重启服务成功')
+		pcall(function()
+			ucic:revert(name)
+		end)
+		if config_snapshot then
+			local restored = nixio.fs.writefile(config_path, config_snapshot)
+			if not restored then
+				log('Failed to restore the previous subscription configuration')
+			end
+		end
+		if service_was_running then
+			luci.sys.call("/etc/init.d/" .. name .. " restart > /dev/null 2>&1 &")
+			log('Restored the proxy service that was running before the update')
 		else
-			luci.sys.call("/etc/init.d/" .. name .. " stop > /dev/null 2>&1 &") -- 不加&的话日志会出现的更早
-			log('停止服务成功')
+			luci.sys.call("/etc/init.d/" .. name .. " stop > /dev/null 2>&1 &")
+			log('Proxy service remains stopped as it was before the update')
 		end
 	end)
 	if direct_bypass then

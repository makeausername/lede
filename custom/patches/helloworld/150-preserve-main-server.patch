diff --git a/luci-app-ssr-plus/luasrc/model/shadowsocksr/subscription_runtime.lua b/luci-app-ssr-plus/luasrc/model/shadowsocksr/subscription_runtime.lua
new file mode 100644
index 0000000..f8cc24c
--- /dev/null
+++ b/luci-app-ssr-plus/luasrc/model/shadowsocksr/subscription_runtime.lua
@@ -0,0 +1,41 @@
+local M = {}
+
+local CONFIG = "shadowsocksr"
+local GLOBAL_TYPE = "global"
+local SERVER_TYPE = "servers"
+
+local function section_exists(cursor, sid)
+	return sid and sid ~= "" and sid ~= "nil" and
+		cursor:get(CONFIG, sid) == SERVER_TYPE
+end
+
+-- Keep the selected main node valid after a subscription refresh. A first
+-- subscription starts from global_server=nil, while a changed subscription
+-- may remove the section that global_server used to reference. In both cases
+-- select the first successfully parsed node before restarting the service.
+function M.ensure_main_server(cursor)
+	local global_sid = cursor:get_first(CONFIG, GLOBAL_TYPE)
+	if not global_sid then
+		return nil, false, "missing-global"
+	end
+
+	local current = cursor:get(CONFIG, global_sid, "global_server") or "nil"
+	if section_exists(cursor, current) then
+		return current, false, "preserved"
+	end
+
+	local first = cursor:get_first(CONFIG, SERVER_TYPE)
+	if first then
+		cursor:set(CONFIG, global_sid, "global_server", first)
+		return first, true, current == "nil" and "selected-first" or "replaced-missing"
+	end
+
+	if current ~= "nil" then
+		cursor:set(CONFIG, global_sid, "global_server", "nil")
+		return nil, true, "disabled-no-server"
+	end
+
+	return nil, false, "no-server"
+end
+
+return M
diff --git a/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua b/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua
index 1af3564..42f1cba 100755
--- a/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua
+++ b/luci-app-ssr-plus/root/usr/share/shadowsocksr/subscribe.lua
@@ -18,6 +18,7 @@ local jsonParse, jsonStringify = luci.jsonc.parse, luci.jsonc.stringify
 local b64decode = nixio.bin.b64decode
 local b64encode = nixio.bin.b64encode
 local URL = require "url"
+local subscription_runtime = require "luci.model.shadowsocksr.subscription_runtime"
 local cache = {}
 local old_groupHash_cache = {}
 local nodeResult = setmetatable({}, {__index = cache}) -- update result
@@ -2957 +2958,11 @@ local execute = function()
-	local globalServer = ucic:get_first(name, 'global', 'global_server', '')
+	local globalServer = ucic:get_first(name, 'global', 'global_server', '')
+	-- Repair the main-server reference before the legacy restart branch below.
+	-- The first successful subscription starts with global_server=nil, and a
+	-- refresh may also remove the section previously referenced by it.
+	local _, selection_changed, selection_reason =
+		subscription_runtime.ensure_main_server(ucic)
+	if selection_changed then
+		ucic:commit(name)
+		log('Main server selection: ' .. selection_reason)
+	end
+	globalServer = ucic:get_first(name, 'global', 'global_server', '')
